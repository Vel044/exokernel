[package]
name = "libos"
version = "0.1.0"
edition = "2021"

# 编译为裸机 EL1 二进制 (不是 UEFI .efi), 被 EL2 外核嵌在 .efi 里
# 用 release 避免 debug 版 UB checks 导致 panic

[profile.dev]
panic = "abort"
opt-level = 1  # 移除未使用代码/死路径, 去掉 UB checks
