# 协作约定

用中文回答所有问题。本项目面向异构机器人，采用外核与用户态 libOS 分层。

## 第一方源码注释

- `abi/src`、`kernel/src`、`libos/src` 使用中文注释。
- 每个模块说明职责和上下层边界；关键结构体字段说明资源、所有权或硬件语义。
- 系统调用链说明 EL0 参数、SVC 进入 EL1 后的校验、返回值及映射或状态变化。
- MMIO 说明 VA/PA 来源、内存属性、volatile 访问和设备寄存器效果。
- DMA 区分 CPU VA、物理地址/DMA address、缓存属性、连续性和设备可见性。
- IRQ 说明 INTID、GIC、Notification、Event Ring 处理和 ACK/EOI 顺序。
- unsafe、裸指针、内联汇编、barrier 和原子内存序说明安全前提或并发目的。
- 应用策略、设备驱动、运行时执行器、第三方库适配层分别解释。
- 不修改 vendor 第三方源码来统一注释；在第一方适配层解释调用和硬件效果。

## 驱动迁移研究

- 区分当前源码事实、设计建议和待验证假设。
- IR 必须保留源码定位、资源生命周期、错误路径和未支持依赖；不得把未知语义静默降为普通内存操作。
- 第一阶段不承诺自动生成可运行的目标驱动，不把单个样例的成功推广为全驱动支持。
